/**
 * Page backgrounds use CSS colors only (see components/PageBackground.jsx).
 * Optional: set finalMessage.backgroundImage in birthdayContent.js for a custom photo on the closing screen only.
 */
export const pageBackgrounds = {};
